# Hermes-USB-Portable — version pin
# Pairs the portable wrapper version with the exact bundled hermes-agent it ships.

portable_version:        portable-v1.8.4
hermes_agent_version:    0.16.0
hermes_agent_upstream:   NousResearch/hermes-agent
hermes_agent_commit:     0bbf325a8f50cbc7cfc74886705e8a67bb521dad
synced_on:               2026-06-16

# Pinned portable runtimes (unchanged in v1.8.4 — all already latest):
#   python:   3.13.14  (python-build-standalone 20260610, astral-sh)
#   node:     24.16.0  (LTS "Krypton")
#   uv:       0.11.21
#   ripgrep:  15.1.0

# v1.8.4 — macOS (Apple Silicon) verification of the v1.8.x stack + hermes-agent re-sync.
#          The on-drive macos-arm64 cache was still the v1.8.1 build (hermes-agent
#          @cc14b747, 13 Jun); setup-unix.sh fetches hermes-agent main HEAD (it has
#          no per-commit pin), so the cache trailed the v1.8.3 record pin @6b76284c.
#          Update check found upstream main +128 commits ahead of @6b76284c (incl.
#          SSL CA-bundle fail-fast guards, refusal-as-error fixes, provider/OAuth
#          account-removal). Forced a clean macOS rebuild onto current main HEAD and
#          re-pinned @6b76284c -> @0bbf325a. Runtimes already newest, unchanged
#          (Python 3.13.14 / Node 24.16.0 / uv 0.11.21 / ripgrep 15.1.0).
#          NO Unix code change — setup-unix.sh / launch.sh untouched (v1.8.3's A/C
#          were Windows-only; B was shared docs). Windows (windows-x64) + Linux
#          (linux-x64) caches NOT rebuilt this round — Linux verifies next and will
#          re-sync its own cache to @0bbf325a.
#          Verified on macOS 27.0 (Apple Silicon, arm64, achupradeeps-MacBook-Air):
#          clean rebuild completes
#          (Source ready -> venv ready -> deps -> Setup Complete; Playwright optional
#          browser install warns, non-blocker); launch.sh --version -> v0.16.0 /
#          Python 3.13.14; doctor exits 0 (NVIDIA NIM connectivity green, SSL CA
#          bundle valid; advisories: dead Kimi key, config v0->v29 migration, portable
#          venv-entry-point false-positive, optional integration keys). [M] -> [1]
#          NVIDIA kept NVIDIA_API_KEY (sha256[0:16]=f179184aa935e782 before and after)
#          and set nvidia / deepseek-ai/deepseek-v4-flash; [11] Back works. Live
#          launch.sh -z -> "MAC OK" (exit 0). New 0bbf325a advisory flags the
#          vendor-prefixed NVIDIA NIM slug — cosmetic; the live call confirms it works.
#          Git re-scanned: no secrets tracked or in history. See CHANGELOG.md.
# v1.8.3 — Windows hardening + storage docs, ADOPTED FROM UPSTREAM techjarves.
#          Pulled two improvements that techjarves/main already had and this fork
#          lacked — both narrow the eventual upstream-merge conflict surface and
#          harden the Windows installer:
#          (A) scripts/setup-windows.ps1 now replaces src\hermes-agent through a
#              lock-tolerant Copy-DirectoryContents helper (adapted from upstream
#              commit b2904ba "fix locked source refresh during setup"). The old
#              Remove-Item + Move-Item aborted setup if any file under src was
#              locked on a re-run (antivirus / Windows Search indexer / a still-
#              running hermes); the helper degrades gracefully (per-child delete
#              then copy).
#          (B) README "Cache & Runtime Footprint" gains a recommended-free-space
#              table (adapted from upstream commit 3c639d7 "docs: clarify portable
#              storage requirements").
#          WINDOWS-ONLY change — setup-unix.sh / launch.sh untouched; macOS and
#          Linux NOT re-run this round (owner verifies macOS first, then Linux).
#          hermes-agent pin unchanged (@6b76284c); runtimes already newest.
#          Verified on Windows 11 (10.0.26200 / LEGION, exFAT): Copy-DirectoryContents
#          unit-tested (normal + locked-file fallback both pass); clean rebuild still
#          completes; launch.bat --version -> v0.16.0 / Python 3.13.14; doctor exits
#          0; live one-shot -> "WIN OK". Git re-scanned: no secrets. See CHANGELOG.md.
# v1.8.2 — Windows verification + exFAT setup fix + hermes-agent source refresh.
#          FIRST clean Windows (x64) rebuild of the v1.8.x stack. Same trap as
#          macOS in v1.8.1: the on-drive windows-x64 cache was still the PRE-v1.8.0
#          stack (Python 3.11.10 / uv 0.6.8 / Node 22.14.0 / rg 14.1.1, built
#          2026-06-06) with ready.flag present — launch.bat would have run the old
#          3.11 stack and never rebuilt. Root-cause bug found on the forced clean
#          rebuild: scripts/setup-windows.ps1 extracts with Windows bsdtar, whose
#          post-extract mtime restore FAILS on the exFAT drive ("Can't restore
#          time: Invalid argument") and makes tar exit NON-ZERO — aborting setup at
#          the Node step (the Expand-Archive fallback then threw on the half-written
#          dir). Fix: pass `-m` (skip mtime restore) to every tar extraction; tar
#          exits 0 on exFAT (proven: node.zip -> exit 1 + 1942 time-errors without
#          -m, exit 0 with -m). Re-synced bundled hermes-agent to upstream main HEAD
#          (cc14b747 -> 6b76284c, +26 commits incl. a Windows-gateway fix + 3
#          security/approval fixes). Runtimes already newest — nothing bumped.
#          Verified on Windows 11 (10.0.26200) / LEGION, exFAT USB: clean rebuild
#          (Py3.13.14/Node24.16.0/uv0.11.21/rg15.1.0); launch.bat --version ->
#          v0.16.0 on Python 3.13.14; doctor exits 0 (NVIDIA NIM green); main + [M]
#          model menus render; [M]->[1] switches config to nvidia/deepseek-v4-flash
#          and keeps NVIDIA_API_KEY; [11] Back returns to main; live one-shot
#          `launch.bat -z` -> "WIN OK". Git re-scanned: no secrets tracked/in
#          history. See CHANGELOG.md.
# v1.8.1 — macOS verification + hermes-agent source refresh. Re-synced bundled
#          hermes-agent to upstream main HEAD (4b646bc2 -> cc14b74; still 0.16.0,
#          +27 commits incl. 3 fix(security) commits). Runtimes already latest
#          (Python 3.13.14, Node 24.16.0, uv 0.11.21, ripgrep 15.1.0 all confirmed
#          newest upstream), so nothing bumped. FIRST clean macOS (arm64) rebuild
#          of the v1.8.0 stack: the on-drive macOS cache was still the pre-v1.8.0
#          Python 3.11.15 build, never rebuilt against v1.8.0's pins. Verified on
#          macOS 27.0 / Apple Silicon (achupradeeps-MacBook-Air):
#          launch.sh --version -> v0.16.0 on Python 3.13.14; doctor exits 0;
#          interactive menu renders + exits clean; live NVIDIA deepseek-v4-flash
#          one-shot returns "MAC OK". Git re-scanned: no secrets tracked/in history.
#          See CHANGELOG.md.
# v1.8.0 — Whole-stack update to latest. Re-synced bundled hermes-agent to upstream
#          main HEAD (56236b16 -> 4b646bc2; still 0.16.0, newer commits/fixes) and
#          bumped every portable runtime: Python 3.11.15 -> 3.13.14 (within
#          hermes-agent's requires-python >=3.11,<3.14), Node 22.14.0 -> 24.16.0 LTS,
#          uv 0.7.8 -> 0.11.21, ripgrep 14.1.1 -> 15.1.0. Also fixed the Windows
#          setup pin pointing at the ABANDONED `indygreg/python-build-standalone`
#          (3.11.10) — now `astral-sh` 3.13.14, matching unix. Motivation: shrink the
#          CVE/vuln surface of the bundled stack. Verified on Linux (AST-WKS-571):
#          full rebuild on Python 3.13.14, hermes runs + live NVIDIA one-shot OK.
#          Git re-scanned: no secrets tracked/in history. See CHANGELOG.md.
# v1.7.0 — Models: added "NVIDIA — DeepSeek V4" to the model switcher (launch.sh
#          [M] menu option [1], launch.bat parity). Wires hermes-agent's native
#          `nvidia` NIM provider (reads NVIDIA_API_KEY, base_url built in) so the
#          drive has a FREE working model out of the box — important because the
#          previous default (kimi-for-coding) is quota-exhausted (HTTP 403).
#          Default model is `deepseek-ai/deepseek-v4-flash` (verified: hermes
#          one-shot returns a real answer on Linux). `deepseek-ai/deepseek-v4-pro`
#          is selectable but NOT default — it runs in full thinking mode and
#          stalls past Hermes' 180s stream-stale limit with large context. See
#          CHANGELOG.md.
# v1.6.0 — Linux first-run fix: `launch.sh` aborted on Linux because the drive is
#          exFAT, which on Linux cannot store symlinks — so `tar -x` of portable
#          Python/Node (both symlink-heavy) died with "Cannot create symlink:
#          Operation not permitted", setup never wrote ready.flag, and every launch
#          re-ran the broken setup. scripts/setup-unix.sh now detects no-symlink
#          drives, extracts on local disk and copies back with symlinks
#          DEREFERENCED into real files (exFAT-safe), and builds the venv on local
#          disk (writing venv.path, matching launch.sh's existing rebuild path).
#          ripgrep/uv/source extraction hardened the same way. Verified on Ubuntu
#          26.04 (AST-WKS-571): setup completes, `hermes --version`/`doctor`/menu
#          all run, 38 plugins discovered. macOS/Windows launchers unchanged. See
#          CHANGELOG.md.
# v1.5.0 — README overhaul: rewritten front-page to document every portable-specific
#          feature shipped since v1.0.0 (Obsidian Brain, Chrome Launcher, Dashboard,
#          Safe Eject, OAuth Gemini, Model Switcher, Versioning). Also adds the
#          Obsidian Brain Setup section describing how to open Brain/ as a vault.
#          No launcher code changes — purely documentation + repo hygiene.
# v1.4.1 — launch.bat one-shot fix: `launch.bat -z "a, b (c)"` (any quoted
#          passthrough args) died with "is was unexpected at this time" because
#          `if not "%ARGS%"=="" ( hermes %ARGS% )` expanded the quoted args at
#          parse time. Now detects args via %~1 and runs `hermes !ARGS!` on a
#          bare line with delayed expansion. macOS launch.sh already worked. See CHANGELOG.md.
# v1.4.0 — Windows launcher parity: port the "Google Gemini — Login with Google"
#          (OAuth, google-gemini-cli, no API key) option into launch.bat. It had
#          shipped in launch.sh at v1.2.0 but Windows was stuck on the API-key-only
#          Gemini menu (provider "gemini" + GEMINI_API_KEY) — which silently failed
#          for OAuth users. Model menu now uses set/p so the 2-digit "[10] Back"
#          works. macOS launch.sh unchanged. See CHANGELOG.md.
# v1.3.1 — dashboard launcher fix: start the server detached with --no-open and
#          poll until it actually binds (cold start is ~60-90s on USB) BEFORE
#          opening the browser, so it no longer lands on a connection-refused
#          page. Adds an "already running" fast-path. See CHANGELOG.md.
# v1.3.0 — added "[D] Open Dashboard" launcher option: serves hermes-agent's web
#          UI (FastAPI + prebuilt Vite SPA) at http://127.0.0.1:9119 via
#          `hermes dashboard --skip-build`. Frontend pre-built onto the drive
#          (hermes_cli/web_dist); fastapi/uvicorn ride the [all] extra. See
#          CHANGELOG.md + docs/DASHBOARD.md.
# v1.2.0 — added "Google Gemini — Login with Google" model option (OAuth, no API
#          key) wiring hermes-agent's native google-gemini-cli provider into the
#          launcher's model menu. See CHANGELOG.md + docs/GEMINI-LOGIN.md.
# v1.1.0 — added "Eject USB Safely" launcher option + scripts/safe-eject.sh
#          (save sessions -> organise/verify Brain -> sync -> eject). See CHANGELOG.md.

# Notes:
# - The hermes-agent source lives in src/ (gitignored); the launchers download it
#   from upstream main on first run. This file records the commit it was last
#   synced/verified against, so the wrapper version is reproducible.
# - Bump portable_version (semver) when the launchers/scripts change; update
#   hermes_agent_commit when you sync source. See docs/UPDATING.md.
